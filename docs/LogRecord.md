# LogRecord

The log record class.

## Example(s)

```php
<?php

use miBadger\Log\LogRecord;

/**
 * Returns the level.
 */
$logRecord->getLevel();

/**
 * Returns the message.
 */
$logRecord->getMessage();

/**
 * Returns the context.
 */
$logRecord->getContext();
```
